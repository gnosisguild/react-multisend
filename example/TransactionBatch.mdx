---
name: Transaction Batch
route: /transaction-batch
---

import { Playground, Props } from 'docz'
import { TransactionBatch } from './TransactionBatch'
import { ProvideMultiSendContext, createTransaction } from '../src'
import * as classNames from './plain.module.css'

# Transaction Batch Example

<Playground>
  {() => {
    const [batch, setBatch] = React.useState([createTransaction('sendFunds')])
    return (
      <ProvideMultiSendContext
        safeAddress="0x1076f084A3703E1701a1a97F837906e56370D4f9"
        network="4"
      >
        <TransactionBatch
          value={batch}
          onChange={setBatch}
          classNames={classNames}
        />
      </ProvideMultiSendContext>
    )
  }}
</Playground>
