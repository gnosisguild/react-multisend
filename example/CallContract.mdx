---
name: Call Contract
route: /call-contract
---

import { Playground } from 'docz'
import { CallContract } from './CallContract'

# Call Contract Example

<Playground>
  {() => {
    const [tx, setTx] = React.useState(undefined)
    return <CallContract value={tx} onChange={setTx} network="4" />
  }}
</Playground>
