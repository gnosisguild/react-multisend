---
name: Transfer Collectible
route: /transfer-collectible
---

import { Playground, Props } from 'docz'
import { TransferCollectible } from './TransferCollectible'
import {
  ProvideSafeCollectibles,
  createTransaction,
  TransactionType,
} from '../src'

# Transfer Collectible Example

<Playground>
  {() => {
    const [tx, setTx] = React.useState(
      createTransaction(TransactionType.transferCollectible)
    )
    return (
      <ProvideSafeCollectibles
        address="0x1076f084A3703E1701a1a97F837906e56370D4f9"
        network="4"
      >
        <TransferCollectible value={tx} onChange={setTx} />
      </ProvideSafeCollectibles>
    )
  }}
</Playground>
